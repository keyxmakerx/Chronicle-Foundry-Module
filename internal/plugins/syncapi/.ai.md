# Sync API Plugin

## Purpose

Provides a versioned REST API (v1) for external tool integration with Chronicle.
External clients such as Foundry VTT modules and custom scripts use these
endpoints to read and write campaign data programmatically. Also provides a
bidirectional entity sync protocol, API key management, request logging,
security event tracking, and an admin monitoring dashboard.

## Tier

**Plugin** -- full feature app with handlers, service, repository, middleware,
and admin templates.

## Files

| File | Role |
|------|------|
| `routes.go` | Route registration: admin, campaign key management, and API v1 |
| `api_handler.go` | Entity CRUD, relations, and bidirectional sync endpoint |
| `calendar_api_handler.go` | Calendar structure, events, date management, import/export |
| `map_api_handler.go` | Map, drawing, token, layer, and fog CRUD |
| `media_api_handler.go` | Media file list/upload/delete with signed URLs |
| `sync_handler.go` | Sync mapping CRUD (Chronicle-to-external ID mappings) |
| `middleware.go` | API key auth, permission checks, campaign match, rate limiting, addon gating |
| `service.go` | Business logic: key generation (bcrypt), auth, logging, security, IP blocklist |
| `model.go` | Domain types: APIKey, APIRequestLog, SecurityEvent, IPBlock, SyncMapping |
| `repository.go` | MariaDB persistence for all sync API data |

## How It Works

### Authentication

API keys use a prefix-lookup + bcrypt-verify scheme:

1. Client sends `Authorization: Bearer chron_<hex>` header.
2. Middleware extracts the 8-char prefix, looks up the key row by prefix.
3. Full key is verified against the stored bcrypt hash.
4. Key must be active and not expired.

Keys are scoped to a single campaign and carry permissions (`read`, `write`, `sync`).

### Device Fingerprint Binding

Clients may send `X-Device-Fingerprint` header. On first use the fingerprint is
bound to the key. Subsequent requests from a different fingerprint are rejected
with a security event logged. This prevents key sharing across devices.

### Rate Limiting

Fixed-window counter per API key per minute. The limit is configurable per key
(default 60, max 1000). Response headers `X-RateLimit-Limit` and
`X-RateLimit-Remaining` are set on every response. Exceeding the limit returns
429 with `Retry-After: 60`.

### Addon Gating

Calendar and maps API endpoints are gated behind `RequireAddonAPI` middleware,
which checks whether the addon is enabled for the campaign. Returns 404 when
disabled, 503 when the check fails (fail-closed).

### Middleware Stack

API v1 requests pass through: `RequireAPIKey` (auth + IP blocklist + request
logging) -> `RateLimit` -> `RequireCampaignMatch` -> `RequirePermission` ->
optionally `RequireAddonAPI`.

## API Endpoints (v1)

All under `/api/v1/campaigns/:id` with API key auth.

### Entity Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/` | read | Campaign details |
| GET | `/entity-types` | read | List entity types |
| GET | `/entity-types/:typeID` | read | Get entity type |
| GET | `/entities` | read | List entities (paginated, ?q=&type_id=&page=&per_page=) |
| GET | `/entities/:entityID` | read | Get single entity (privacy enforced) |
| GET | `/entities/:entityID/relations` | read | List entity relations |
| POST | `/entities` | write | Create entity |
| PUT | `/entities/:entityID` | write | Update entity |
| PUT | `/entities/:entityID/fields` | write | Update custom fields only |
| DELETE | `/entities/:entityID` | write | Delete entity |

### Sync Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| POST | `/sync` | sync | Bidirectional sync (pull since + push changes) |
| GET | `/sync/mappings` | sync | List ID mappings (paginated) |
| GET | `/sync/mappings/:mappingID` | sync | Get single mapping |
| POST | `/sync/mappings` | sync | Create ID mapping |
| DELETE | `/sync/mappings/:mappingID` | sync | Delete ID mapping |
| GET | `/sync/lookup` | sync | Lookup by Chronicle or external identity |
| GET | `/sync/pull` | sync | Pull mappings modified since timestamp |

### Calendar Endpoints (requires calendar addon)

Read (read permission): GetCalendar, GetCurrentDate, ListEvents, GetEvent, ExportCalendar.
Write (write permission): CreateEvent, UpdateEvent, DeleteEvent, UpdateCalendarSettings,
UpdateMonths, UpdateWeekdays, UpdateMoons, UpdateEras, AdvanceDate, SetDate, AdvanceTime,
ImportCalendar.

### Media Endpoints

| Method | Path | Permission | Description |
|--------|------|------------|-------------|
| GET | `/media` | read | List media (paginated) |
| GET | `/media/stats` | read | Storage statistics |
| GET | `/media/:mediaID` | read | Get file metadata with signed URL |
| POST | `/media` | write | Upload file (multipart, 10MB limit) |
| DELETE | `/media/:mediaID` | write | Delete file |

### Map Endpoints (requires maps addon)

Full CRUD for maps, drawings, tokens, layers, and fog of war. Read endpoints
need `read` permission; write endpoints need `write` permission. Includes
optimized `PATCH .../tokens/:tokenID/position` for real-time drag sync.

## Sync Protocol

The `POST /sync` endpoint supports bidirectional sync in a single request:

- **Pull**: set `since` (RFC3339 timestamp) to receive entities modified after
  that time. Paginated internally (max 10 pages of 100). Returns `has_more` flag.
- **Push**: set `changes` array with `{action, entity_id, ...}` objects.
  Actions: `create`, `update`, `delete`. Max 2000 changes per request.
- **Response**: `server_time` for use as the next `since` value, `entities`
  array of pulled data, and `results` array with per-change status.

## Admin Routes

Under `/admin/api` (site admin only): dashboard, request logs, security events
(with resolve), IP blocklist management (block/unblock), and key toggle/revoke.

## Dependencies

- **Uses:** entities, campaigns, calendar, maps, media, relations, addons plugins
- **Used by:** Foundry VTT sync module, external scripts, admin dashboard

## Notable Design Decisions

- Keys use bcrypt hashing with prefix-based lookup to avoid full-table scans.
- Rate limiter is in-memory (not Redis) for simplicity; acceptable for
  single-instance deployments.
- Request logging and security events are fire-and-forget goroutines to avoid
  blocking API responses.
- IDOR protection on every endpoint: entity/event/media ownership verified
  against the key's campaign before any operation.
- WebSocket authentication reuses the same key validation path via
  `AuthenticateKeyForWS`, returning owner-level role for API key holders.
- Media API responses include HMAC-signed URLs when a signer is configured,
  with 1-hour expiry for direct file access.

## Current State

- [x] Entity CRUD API (read/write)
- [x] Bidirectional sync with pull/push
- [x] Sync mappings (Chronicle-to-external ID mapping)
- [x] Calendar API (full CRUD + import/export)
- [x] Media API (list/upload/delete with signed URLs)
- [x] Map API (maps, drawings, tokens, layers, fog)
- [x] API key auth with bcrypt + prefix lookup
- [x] Device fingerprint binding
- [x] Rate limiting with response headers
- [x] Addon gating for calendar and maps
- [x] Request logging and security events
- [x] Admin monitoring dashboard
- [x] IP blocklist management
