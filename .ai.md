# Chronicle Sync — Foundry VTT Module

## Purpose

Bidirectional real-time sync between Chronicle and a Foundry VTT (v12-v14)
world, over REST + WebSocket.

## Architecture

```
module.mjs (entry point)
  └─ SyncManager (sync-manager.mjs)
       ├─ ChronicleAPI (api-client.mjs)    ← REST + WebSocket client
       ├─ JournalSync (journal-sync.mjs)   ← Entity ↔ JournalEntry
       ├─ MapSync (map-sync.mjs)           ← Map + markers/drawings/tokens/fog/layers ↔ JournalEntry (image page) with overlay
       ├─ CalendarSync (calendar-sync.mjs) ← Calendar events/date ↔ Calendaria/SimpleCalendar
       ├─ ShopWidget (shop-widget.mjs)     ← Shop entity inventory in Foundry UI
       ├─ NoteSync (note-sync.mjs)        ← Chronicle Notes ↔ JournalEntry
       ├─ ItemSync (item-sync.mjs)        ← Actor inventory ↔ Chronicle relations
       ├─ SyncDashboard (sync-dashboard.mjs) ← 11-tab ApplicationV2 status/management UI
       └─ ActorSync (actor-sync.mjs)       ← Character entity ↔ Actor
            └─ generic-adapter.mjs          ← API-driven adapter (all systems)
```

### Data flow

1. GM starts Foundry → `module.mjs` initializes SyncManager.
2. `SyncManager.start()` validates config, initializes modules, connects WS.
3. WebSocket connects → initial sync (pulls mappings since `lastSyncTime`).
4. Live updates: WS messages routed to modules; REST for CRUD.
5. Foundry hooks detect local changes, push to Chronicle via REST.

`_syncing`-guard convention, real-time-calendar date-push guard, calendar
sub-resource display-only rule: see CLAUDE.md → Code Conventions.
`scripts/_realtime-date-guard.mjs` is shared by `calendar-sync.mjs`'s three
hook-triggered pushes and `sync-dashboard.mjs`'s manual push button, with a
one-time-per-session GM notice as a shared module-level singleton.

### Applied-date confirmation

Chronicle's sync chip distinguishes what Foundry *saw* (the `GET
/calendar/date` beacon) from what it *applied*. `calendar-sync.mjs`'s
`_setLocalDate` (choke point for both the poll and WS
`calendar.date.advanced` apply paths) resolves `Promise<boolean>` reporting
whether a real local setter ran without throwing. Both sites then call
`_applied-date-confirm.mjs`'s `confirmAppliedDate(api, {year, month, day})` →
`POST /calendar/date/confirm`, only on `true`. A 404/405 (older Chronicle) is
tolerated and logged once per session. `tools/test-applied-date-confirm.mjs`.

### Sync wire rules

- `sync-manager.mjs`'s `_onSyncStatus` reads `msg.status ?? msg.payload?.status`
  (emit shapes vary by message).
- SimpleCalendar shares Calendaria's structure-mismatch guard; fails open on
  an unreadable structure.
- Dashboard sync badge: pure four-state classifier in
  `scripts/_calendar-sync-state.mjs` (`in-sync` / `date-drift` /
  `incompatible-structures` / `paused`).
- Visibility toggles: `POST /entities/:id/reveal`, not a bare `PUT` (400s
  without a name).
- Item relations: flat `DELETE|PUT /relations/:relationId`, snake_case bodies.

Pinned by `tools/test-sync-wire-fix.mjs`, `tools/test-calendar-sync-state.mjs`.
Calendaria back-catalog note mappings are per-GM-user, not per-world (#95).

## Files

| File | Purpose |
|------|---------|
| `module.json` | Module manifest. Foundry v12-v14. Optional deps: Calendaria, Monk's Enhanced Journal |
| `scripts/module.mjs` | Entry point: settings on `init`, SyncManager on `ready` (GM only) |
| `scripts/settings.mjs` | 8 primary world-scoped settings (apiUrl, campaignId, syncEnabled, syncJournals/Maps/Calendar/Notes/Characters) plus internal/advanced settings (lastSyncTime, detectedSystem, userMappings, syncExclusions, syncDirections, syncPermissions, defaultOwnership, dmOnlyHidden, conflictResolution, autoSync, excludedTags, excludedNamePattern, calendarSyncExclusions, skipIncomingSanitization, calendarAnnounce*) |
| `scripts/constants.mjs` | FLAG_SCOPE, MODULE_ID |
| `scripts/api-client.mjs` | REST (Bearer) + WebSocket (auto-reconnect, message queue, event emitter) |
| `scripts/sync-manager.mjs` | Orchestrator: owns API client, routes WS, sync mapping lookups |
| `scripts/journal-sync.mjs` | Entity ↔ JournalEntry sync; Monk's Enhanced Journal support. Permission mapping: `visibility:'custom'`+`entity_permissions` ↔ per-user Foundry ownership (`view`→OBSERVER, `edit`→OWNER); unmapped Foundry users are skipped (not over-shared) and reported; `tag_grants` can't map to users so they surface as a warning. Fail-closed: ambiguity resolves toward less player visibility |
| `scripts/map-sync.mjs` | Materializes maps as JournalEntries (image page) in "Chronicle Maps"; markers/drawings/tokens/fog/layers as `MapViewerSheet` overlays, not Scene objects. Idempotent by `mapId` flag. Visibility filtered at materialization + render |
| `scripts/_map-flag-filter.mjs` | `isMarkerSafeForPlayerFlags`/`isDrawingSafeForPlayerFlags`/`isTokenSafeForPlayerFlags`: keep `dm_only`/restricted/hidden map sub-resources out of shared JournalEntry flags (no per-recipient delivery there). `tools/test-map-flag-filter.mjs` |
| `scripts/map-viewer.mjs` | `MapViewerSheet` ApplicationV2: image + SVG overlay, sheet for image-type pages via `flags.core.sheetClass` |
| `scripts/update-info.mjs` | "Update Source" diagnostic dialog — see "Chronicle Integration" below |
| `scripts/sync-calendar.mjs` | "Sync Calendar" editor (ApplicationV2, GM-only): year/month/day view + moon strip + writable note form (drag-select, recurrence presets); empty-state Calendaria import. Weather/structure editing not implemented |
| `scripts/sync-calendar-validation.mjs` | Pure validation rule engine → `null` or `{severity, code, message, ...}`. `tools/test-sync-calendar-validation.mjs` |
| `scripts/sync-calendar-note-form.mjs` | Pure form ↔ Calendaria API translation; round-trips `conditionTree`. `tools/test-sync-calendar-note-form.mjs` |
| `scripts/sync-calendar-moon-strip.mjs` | Pure moon-strip view-model + convergence detection. `tools/test-sync-calendar-moon-strip.mjs` |
| `scripts/sync-calendar-condition-builder.mjs` | Pure conditionTree preset composer/validator (root-must-be-group rule). `tools/test-sync-calendar-condition-builder.mjs` |
| `scripts/sync-calendar-diagnostics.mjs` | `buildCalendarDiagnostics(input)` → Markdown report, backs both "Copy diagnostics" buttons. `tools/test-sync-calendar-diagnostics.mjs` |
| `scripts/sync-calendar-import-from-calendaria.mjs` | Calendaria → Chronicle calendar-create transform, `IMPORT_WIRE_VERSION: 1`. `tools/test-sync-calendar-import-from-calendaria.mjs` |
| `chronicle-package.json` | Repo-root serving descriptor (schema v1) — see "Chronicle Integration" |
| `tools/check-package-descriptor.mjs` | CI: validates descriptor against `module.json`, via `.github/workflows/check-descriptor.yml` |
| `tools/test-lang-expand.mjs` | CI guard for `lang/en.json` — see F-LANG-1 |
| `tools/test-template-roots.mjs` | CI guard: PARTS templates render exactly one root — see F-PR3-4 |
| `scripts/calendar-sync.mjs` | Adapter pattern for Calendaria/SimpleCalendar; 0-↔1-indexed date conversion |
| `scripts/_realtime-date-guard.mjs` | `shouldSkipDatePush`, `isRealTimeRejection` (422 backstop), `notifyRealTimePushPaused`. `tools/test-realtime-date-signal.mjs` |
| `scripts/_applied-date-confirm.mjs` | See "Applied-date confirmation" above. `tools/test-applied-date-confirm.mjs` |
| `scripts/_calendar-sync-state.mjs` | `classifyCalendarSyncState`; priority module-paused > structure mismatch > date drift > in-sync, fails open. `tools/test-calendar-sync-state.mjs` |
| `scripts/shop-widget.mjs` | Shop window (Application v1): context menu, drag-to-sheet, real-time updates |
| `scripts/actor-sync.mjs` | Actor ↔ character entity sync, adapter loading. `is_private` ↔ `prototypeToken.hidden` NPC visibility syncs bidirectionally. Deleting from Chronicle unlinks the Actor (data preserved); deleting the Actor in Foundry deletes the Chronicle entity. Player Character Claiming addon-aware: PC sub-type routing, `owner_user_id` gated on addon state, one-time hint when addon is off but player-owned actors exist |
| `scripts/item-sync.mjs` | Actor inventory ↔ Chronicle "Has Item" relations (quantity, equipped) |
| `scripts/note-sync.mjs` | Chronicle Notes ↔ JournalEntries in "Chronicle Notes" folder tree; sharing → ownership |
| `scripts/adapters/generic-adapter.mjs` | API-driven adapter: `foundry_path` annotations from `/systems/:id/character-fields` → field mappings, for any system |
| `scripts/sync-dashboard.mjs` | 11-tab ApplicationV2 dashboard (Overview, Config, Entities, Shops, Maps, Characters, Calendar, Issues, Members, Notes, Status). Members tab: per-member mapping dropdown + matched/UNMATCHED badge. Status tab: Copy Calendar Diagnostics + DM-only-hidden counter |
| `scripts/_member-mapping.mjs` | `buildMemberRows` view-model for the Members tab. `tools/test-permission-sync.mjs` |
| `templates/sync-dashboard.hbs`, `templates/shop-window.hbs` | Handlebars templates |
| `styles/chronicle-sync.css` | Status indicator + shop window styles |
| `styles/sync-calendar-pr3.css` | Sidecar: moon-strip grid, recurrence section, import banner; loaded after `chronicle-sync.css` for cascade priority |
| `lang/en.json` | UI string localization |
| `TESTING.md` | E2E checklist (journals, maps, calendar, shops, characters) |
| `CALENDARIA-INTEGRATION.md` | Calendaria data-model reference; read before touching any `createJournalEntry` handler — see F-SYNC-1 |

## API Endpoints Used

Full request/response schemas, WebSocket protocol, auth: **API-CONTRACT.md**.

| Feature | Endpoints | Module |
|---------|-----------|--------|
| Auth | Bearer token header (REST), `?token=` query (WS) | `api-client.mjs` |
| Systems | `GET /systems`, `GET /systems/:id/character-fields`, `GET /systems/:id/item-fields` | `sync-manager.mjs`, `generic-adapter.mjs`, `item-sync.mjs` |
| Entities | `GET/POST/PUT/DELETE /entities/*`, `PUT /entities/:id/fields`, `POST /entities/:id/reveal` | `journal-sync.mjs`, `actor-sync.mjs` |
| Entity Types | `GET /entity-types` | `actor-sync.mjs`, `journal-sync.mjs` |
| Entity Permissions | `GET/PUT /entities/:id/permissions` | `journal-sync.mjs` |
| Relations | `GET /entities/:id/relations`, `POST/PUT/DELETE .../relations/*` | `item-sync.mjs`, `shop-widget.mjs` |
| Maps | `GET /maps` | `map-sync.mjs` |
| Map Markers | `GET/POST/PUT/DELETE /maps/:id/markers/*` | `map-sync.mjs` |
| Calendar | `GET /calendar`, `GET/PUT /calendar/date` (`tracks_real_time` gates push), `POST /calendar/date/confirm` (optional), `POST /calendar/advance`, `POST /calendar/advance-time` | `calendar-sync.mjs`, `_realtime-date-guard.mjs`, `_applied-date-confirm.mjs` |
| Calendar Events | `GET/POST/PUT/DELETE /calendar/events/*` | `calendar-sync.mjs` |
| Calendar Sub-Resources | `GET/PUT /calendar/{seasons,moons,eras,event-categories,cycles,festivals,weather}` | Not yet used |
| Calendar Structure | `GET /calendar/structure`, `PUT /calendar/{settings,months,weekdays}`, `GET /calendar/export`, `POST /calendar/import` | Not yet used |
| Notes | `GET/POST/PUT/DELETE /notes/*` (camelCase responses, snake_case requests) | `note-sync.mjs` |
| Sync Mappings | `GET/POST/DELETE /sync/mappings`, `GET /sync/lookup`, `GET /sync/pull`, `POST /sync` | `sync-manager.mjs`, all sync modules |
| Media | `POST /media/upload`, `GET/DELETE /media/:id` | `api-client.mjs` |
| Members | `GET /members` | `sync-manager.mjs` |
| Addons | `GET /addons` | `sync-manager.mjs` |

## WebSocket Message Types

Full protocol: API-CONTRACT.md.

**Handled:** `entity.*`/`entity_type.*` → JournalSync; `marker.*` → MapSync;
`calendar.event.*`, `calendar.date.advanced` → CalendarSync; `note.*` →
NoteSync; `relation.*` → ItemSync; `sync.status` →
`SyncManager._onSyncStatus` (fires one-time initial sync on first `connected`).

**Calendar blackout** — see CLAUDE.md → "Calendar blackout" and
API-CONTRACT.md → "CALENDAR BLACKOUT": all calendar routes answer `503
calendar_rebuilding`; every `calendar.*` WS type is dormant.

**Not yet handled:** `sync.error`, `sync.conflict`. Any other `calendar.*`
type logs one `console.debug` per type per session (`default:` branch).

**Calendar sub-resources** (`scripts/_calendar-subresources.mjs`): display-only,
no Foundry document writes. No structure auto-apply on
`calendar.structure.updated` (would silently re-date every Calendaria note) —
badges the mismatch instead, routed ahead of the sync-disabled guard since
fixing the calendar in Chronicle is what emits this broadcast (the only way
to clear a mismatch pause). No auto-created note on
`calendar.worldstate.changed` (no stable dedupe id; Chronicle-side gap — this
event doesn't reach the wire yet, API-CONTRACT.md → "Gap"). No speculative
weather setter (Calendaria exposes reads only) — probes known setter names,
degrades to a GM chat line. Announcements are GM whispers only
(`CalendarSync._announceToGM`) since Chronicle's `dm_only` gating is
server-side and re-broadcasting would leak it to players.
`tools/test-calendar-subresources.mjs`, `tools/test-calendar-subresource-routing.mjs`.

## Security posture

Chronicle Sync trusts a single operator-configured Chronicle deployment, with
**targeted sanitization at the highest-risk surfaces**: stored HTML, `<img
src>` URLs, dashboard `innerHTML` interpolation. Response field shapes and WS
payload contents beyond the `type` allowlist are not validated.

### Auth surface separation — never cross

| Surface | Auth carrier | URL shape | Audience |
|---|---|---|---|
| REST API (sync) | `Authorization: Bearer ${apiKey}` header | `${apiUrl}/api/v1/campaigns/${campaignId}/...` | All sync modules, dashboard, wizard |
| WebSocket (sync) | `?token=${apiKey}` query param | `wss://...${apiUrl host}/ws?token=...&client=foundry-module` | `api-client.mjs::_doConnect` only |
| Module manifest fetch | Install-URL signed token in URL | `${apiUrl}/api/v1/campaigns/${campaignId}/foundry-vtt/module.json?token=...` | Foundry's update-check flow |
| Module zip download | Install-URL signed token in URL | `${apiUrl}/api/v1/campaigns/${campaignId}/foundry-vtt/module.zip?token=...` | Foundry's installer |

Different secrets, different validation paths — install-URL token is
Chronicle-signed with campaign+expiration claims `/api/v1/` rejects; `apiKey`
is a bearer token `module.json` rejects. Never consolidate them. Pinned by
`tools/test-api-client-security.mjs`.

### Trust posture

Each row is an unfixed-without-it footgun: a tampered Chronicle response could
otherwise reach a player's browser as XSS, a redirect-and-leak, a URL
break-out, or a leaked bearer token.

| Class | What we trust | What we validate | Where | Pin |
|---|---|---|---|---|
| HTML for `JournalEntry.text.content` | Chronicle's bluemonday UGCPolicy at write | Pre-sanitize via `TextEditor.cleanHTML` at ingress | `scripts/_html-sanitizer.mjs` | `tools/test-html-sanitizer.mjs` |
| Image URLs from map sub-resources | Operator-configured `apiUrl` host | `_isAllowedImageHost` rejects full-URL `image_url`/token `image` whose hostname doesn't match `apiUrl`, in `map-sync.mjs` + `map-viewer.mjs`; rejection → blank `<img>` + warning | `scripts/_url-validation.mjs` | `tools/test-url-validation.mjs` |
| User-visible DOM interpolation (e.g. dashboard test results) | Nothing | `replaceChildren` + `createTextNode`, never `innerHTML` of Chronicle data | `scripts/sync-dashboard.mjs::_renderTestResults` | `tools/test-sync-dashboard-xss.mjs` |
| `campaignId` setting value | Operator typed it correctly | `_validateCampaignIdOrThrow` enforces UUID v4 shape before URL interpolation (`fetch`/`uploadMedia`) | `scripts/_settings-validation.mjs` | `tools/test-campaign-id-validation.mjs` |
| Outbound error log entries | Nothing (response body may echo the request) | `_scrubAuthHeaders` redacts `Bearer <token>` / `?token=<value>` before storing | `scripts/api-client.mjs::_scrubAuthHeaders` | `tools/test-api-client-secrets.mjs` |

**Sanitization layers on Chronicle HTML:** (1) Chronicle server-side bluemonday
UGCPolicy on write (primary); (2) Foundry ingress —
`_sanitizeIncomingHTML(html)` wraps `TextEditor.cleanHTML` at six sites in
`journal-sync.mjs`/`note-sync.mjs` (operator escape:
`skipIncomingSanitization`, default off); (3) Foundry render-time
`TextEditor.enrichHTML`, survives even if (1)+(2) fail. Static scan of every
ingestion site: `tools/test-html-sanitizer.mjs`.

**Descriptor schema integrity:** CI (`tools/check-package-descriptor.mjs`)
and runtime (`scripts/module.mjs::_runtimeValidateDescriptor`, GM-gated
`ready` hook, non-fatal `ui.notifications.error` on drift) share
`validateDescriptor(descriptor, moduleJson)`. `tools/test-descriptor-validator.mjs`.

**Auth quick reference:** API key is a Foundry **client** setting (CLAUDE.md →
Code Conventions), masked via `renderSettingsConfig`. Rate-limited
server-side (60 req/min default), campaign-scoped, optional
device-fingerprint lock. See also `chronicle/.ai/conventions.md` security
section and CLAUDE.md → "Working with this project" (tenets T-B1, T-B4).

## Known API Field Discrepancies

API-CONTRACT.md defines canonical names; code uses some accepted aliases.

| Area | Contract Field | Code Uses | Files |
|------|---------------|-----------|-------|
| Entity content (GET) | `content` | `entry_html` | `journal-sync.mjs` |
| Entity content (POST) | `content` | `entry` | `journal-sync.mjs` |
| Entity visibility (GET) | `visibility: "public"` | `is_private: true/false` + `visibility` | `journal-sync.mjs`, `actor-sync.mjs` |
| Sync mapping POST | `type`, `foundry_id` | `chronicle_type`, `external_id`, `external_system`, `sync_direction` | All sync modules |
| Sync lookup query | `?foundry_id=X&type=Y` | `?external_system=foundry&external_id=X` | `sync-manager.mjs` |
| Relations (READ/WS) | — | `sourceEntityId`, `targetEntityId`, `relationType` (camelCase) | `item-sync.mjs`, `shop-widget.mjs` |
| Relations (WRITE) | `target_entity_id`, `relation_type`, `reverse_relation_type` (snake_case) | snake_case | `item-sync.mjs` |
| Calendar events (POST) | `color`, `icon`, `all_day`, `recurrence_*`, `start_hour`, `start_minute` | Not sent (simplified fields) | `calendar-sync.mjs` |

Relations are asymmetric on purpose: reads/WS are camelCase
(`internal/widgets/relations/model.go`), writes are snake_case
(`syncapi/api_handler.go`); routes are flat (`/relations/:relationId`), not
nested under `/entities/:id`.

## Known Limitations

- Map drawings/tokens/fog/layers: read-only SVG overlays in `MapViewerSheet`;
  only markers are bidirectional.
- SimpleCalendar events: journal notes only, no CRUD hooks (structure guard
  still applies).
- Shop icon field always null (`shop-widget.mjs`). Only the active Foundry
  scene syncs. Full sync runs only for GM users.
- PF2e character sync: only HP and name sync back (rest derives from items/rules).
- Custom system character sync needs `foundry_path` annotations on character
  preset fields, else the generic adapter returns null; dnd5e/pf2e have
  hand-written adapters. System matching is fully API-driven via each
  installed system's `foundry_system_id`.
- Conflict handling: `ConflictError` (409, stale `expected_updated_at`)
  resolved per `conflictResolution`: `chronicle` (re-pull), `foundry` (force
  push), `newest` (compare timestamps).

Tests: `tools/test-permission-sync.mjs`, `tools/test-build-ownership.mjs`,
`tools/test-pc-claiming.mjs`, plus the per-module suites in `TESTING.md`.

## Chronicle Integration — Install & Updates

Foundry installs/updates this module from **Chronicle**, not GitHub: the
`foundry_vtt` sub-plugin serves each campaign a signed per-campaign install
URL pinned to the version the campaign owner picked. Chronicle pulls each
version from this repo's **GitHub releases** (or GitHub's source zip if a
release has no attached asset) — releasing means publishing a GitHub release
tagged e.g. `0.1.30`; nothing is uploaded to Chronicle by hand.

**Install flow:** owner pins a version in Chronicle's campaign settings →
Chronicle issues a signed manifest URL
(`/api/v1/campaigns/{id}/foundry-vtt/module.json?token={signed}`) → pasted
into Foundry's Install Module dialog → Foundry GETs it, gets `module.json`
with `manifest`/`download` rewritten to Chronicle URLs → follows `download`,
installs → Foundry stores that manifest URL and reuses it for future update
checks.

**`chronicle-package.json`** (repo root) is the serving descriptor Chronicle's
`PostInstallHook` reads from the release zip: manifest file location
(`package.moduleJsonPath`), fields to rewrite (`serving.rewriteFields` —
`manifest`+`download`), URL shape, and whether signed tokens are required.
Absent/invalid → Chronicle falls back to hardcoded defaults. CI validates it
every push/PR (`tools/check-package-descriptor.mjs`); bumping
`module.json#/id` requires updating `chronicle-package.json#/package/id` in
the same commit (CI-enforced). Full contract: API-CONTRACT.md →
"Chronicle-served Module Distribution".

The on-disk `module.json`'s `manifest`/`download` deliberately still point at
GitHub (pre-rewrite values, so a directly-obtained zip still works and
`PackagesPlugin` can auto-pull releases) — don't "fix" these; `serving.rewriteFields`
does the rewrite at serve time.

**Update Source dialog** (`scripts/update-info.mjs`, Game Settings → Module
Settings → Chronicle Sync → Update Source): shows the install-time manifest
URL, classifies it (chronicle/github/unknown) via the locked regex
`/api/v1/campaigns/.../foundry-vtt/module.json`, plus a manual "Check
Chronicle for updates" button. Keep this file and `chronicle-package.json` in
sync if the URL shape changes. A GM-only, fire-and-forget startup probe
(`surfaceManifestRecoveryIfNeeded()` in `module.mjs`'s `ready` hook) re-checks
the URL once per session; on an `auth`-category result it shows a sticky
`ui.notifications.error` telling the GM to reinstall — other outcomes stay
silent (covered by the manual dialog or not GM-actionable).

**Error classification** (`update-info.mjs`), in priority order: trust
Chronicle's `body.category` when it's one of the five wire-contract values
(`auth | config | not_found | validation | internal`); else HTTP-status
fallback (401/403→auth, 404→not_found, 5xx/else→internal); else Foundry-only
`network` (fetch threw) / `parse` (bad JSON or missing `version`).

| Category | Triggered by | Icon | Hue |
|---|---|---|---|
| `auth` | `invalid_token`, 401/403 w/o body | `fa-key` | red |
| `config` | `no_package_registered`, `pinned_version_not_installed`, `token_not_initialized`, `no_version_available` | `fa-gear` | amber |
| `not_found` | `campaign_not_found`, 404 w/o body, no install-time URL recorded | `fa-magnifying-glass` | amber |
| `validation` | `descriptor_invalid` | `fa-triangle-exclamation` | amber |
| `internal` | `module_json_missing`, `ErrInternal`, 5xx w/o body | `fa-server` | red |
| `network` | fetch threw (DNS/TLS/CORS/firewall) | `fa-plug-circle-xmark` | slate |
| `parse` | bad JSON, 200 OK missing `version` | `fa-file-circle-question` | slate |

Category names are snake_case on the wire, mirrored in CSS classes
(`result-auth`, `result-not_found`, ...). No local lookup table — the dialog
trusts `body.category` rather than duplicating Chronicle's mapping. Canonical
codes: API-CONTRACT.md → "Chronicle-served Module Distribution" → "Error code
catalog".

**Token rotation:** owner-rotated; old token immediately returns `403
invalid_token/auth`. No in-place repair — recovery is uninstall + reinstall
via a fresh install URL. The version pin survives rotation.

**Releases:** Actions → Release → Run workflow with the version (X.Y.Z).
`.github/workflows/release.yml` tags main and publishes the release with
generated notes and no attachments. Chronicle installs the source zip on Check
for updates, and its install hook writes the tag into `module.json`, so the
`version` on main is never read.

## Foundry conventions and footguns

- **F-PR2-1 — ApplicationV2 `actions` only delegates `click`.** Non-click
  events must be wired directly in `_onRender` via `addEventListener`; adding
  them to the `actions` map is silently dropped, no error.
- **F-PR2-2 — hook re-render wipes in-progress form state.** A mid-edit
  Calendaria hook can trigger a re-render; write every keystroke to a
  class-side `_formData` object (`#wireFormInputs` in `_onRender`) instead of
  reading DOM values, and throttle re-renders (250ms, `#scheduleRerender`).
- **F-PR2-3 — test bootstrap for `foundry.applications.api` files.** Importing
  `calendar-sync.mjs` (or anything pulling `settings.mjs` →
  `update-info.mjs`/`sync-calendar.mjs`) from a unit test throws
  `ReferenceError: foundry is not defined`; stub
  `globalThis.foundry/game/Hooks` before the dynamic `import()`. Canonical
  stub: `tools/test-calendar-sync-visibility.mjs`.
- **F-PR2-4 — i18n key collisions are silent.** Two `registerMenu` calls can't
  share an i18n key (Foundry returns the first match). Settings-menu entries
  live under `CHRONICLE.Settings.<menuId>Menu.*`, not
  `CHRONICLE.Settings.<menuId>.*` (the boolean toggle's namespace).
- **F-PR3-1 — Handlebars helpers need explicit registration.** `eq`, `neq`,
  `lt`, `gt`, `json`, `timeAgo`, etc. are registered in `module.mjs`'s `init`
  hook; register new ones alongside them.
- **F-PR3-2 — Calendaria `conditionTree` root must be a group.** A root other
  than `{type:'group', mode, children}` is silently ignored (note never
  fires, no error). Validator: `sync-calendar-condition-builder.mjs`; save
  path blocks on `Errors.RootMustBeGroup`/`EmptyGroup`/`NotAnObject`.
- **F-PR3-4 — ApplicationV2 PARTS templates need exactly one root element.**
  `_parsePartHTML` rejects a template rendering more or fewer than one root,
  only at `app.render()` time — and the failing branch depends on which
  `{{#if}}` path fires. Wrap the whole template body in one root element so
  every branch emits exactly one. CI guard: `tools/test-template-roots.mjs`
  (recurses every branch, asserts max-root-count == 1).
- **F-CAL-RAIL-SCROLL — the single-root wrapper needs a height-constrained
  flex column.** Consequence of F-PR3-4: without `display:flex;
  flex-direction:column; height:100%; min-height:0` on `.sync-calendar-root`
  (`styles/chronicle-sync.css`), a `flex:1` scroll region inside it overflows
  instead of scrolling. Any new single-root wrapper with an internal scroll
  region needs the same rule.
- **F-LANG-1 — `lang/en.json` can't mix a dotted-flat key with a sibling
  literal-key string.** `expandObject` throws writing a property onto a
  string when both `parent.X = "..."` and `parent.X.Y = "..."` exist as
  siblings, and **rejects the entire lang file** — every `CHRONICLE.*` key
  then renders raw, with no error at lint time. Fix: nest the option set
  under a sibling key (e.g. `VisibilityOption: {Visible, Hidden, Secret}`),
  keep the field label flat. CI guard: `tools/test-lang-expand.mjs`.
- **F-PR3-3 — Sync Calendar editor reaches Chronicle via the public API.** No
  direct SyncManager reference; calls
  `game.modules.get('chronicle-sync').api.getAPI()`, avoiding a coupling to
  SyncManager lifecycle.
- **F-SYNC-1 — JournalSync must skip JournalEntries owned by another sync
  domain.** `JournalSync`, `NoteSync`, `CalendarSync` share the global
  `createJournalEntry` hook (`JournalSync` first). Calendaria, SimpleCalendar
  and `NoteSync` all store notes as JournalEntries. `JournalSync` used to push
  any journal lacking an `entityId` flag to `POST /entities` with
  `entity_type_id: 0`, which Chronicle resolves to the campaign's first
  entity type (almost always "Character") — e.g. Calendaria's auto-seeded
  festival notes showed up as Character entities. Fix: skip when
  `isCalendarNoteJournal(journal)` (from `calendar-sync.mjs`) or
  `_isHandledByNoteSync(journal)`, mirroring `_isHandledByActorSync`. Cleanup
  order matters: unlink the journal locally before deleting the bad entity
  server-side, never the reverse (deleting first cascades to deleting the
  Foundry note via `entity.deleted`; unlinking without deleting leaves an
  orphan `ActorSync` re-pulls as an Actor) — see the macro in
  `CALENDARIA-INTEGRATION.md`. Tested at `tools/test-calendar-note-journal.mjs`.

## Architectural Decision Log

### A-1: Chronicle-served module distribution
Release artifacts are served by Chronicle on a per-campaign signed URL, not
GitHub Releases or Foundry's Package Repository — lets the operator pin
different campaigns to different versions and control rollout from
Chronicle's admin UI, without exposing unbounded public install URLs.

### A-2: Descriptor-driven Chronicle serving
Chronicle reads `chronicle-package.json` from the release zip to learn how to
serve this module, falling back to hardcoded defaults if absent — keeps
Chronicle generic so any future Foundry module can register its own
descriptor without Chronicle code changes.

### A-3: Foundry stores the Chronicle URL post-install; updates loop back to Chronicle
Foundry's install flow stores the per-campaign manifest URL locally and
always checks it for updates, never GitHub — keeps the campaign owner's
version pin authoritative. Risk: Foundry's Package Repository also lists this
module under its GitHub URL and may prompt to switch (README's "Updating in
Foundry").

### A-4: Update Source dialog is a manual check, not an autopatcher
It's a diagnostic, not part of the runtime update loop — Foundry's native
Update All is the production path, and auto-correcting a deliberately
GitHub-installed instance would be too aggressive.
