# Chronicle Sync — Foundry VTT Module

## Purpose

The Chronicle Sync module for Foundry VTT enables bidirectional real-time sync
between a Chronicle instance and a Foundry VTT game world. It runs as a Foundry
v12-v14 module, connecting via REST API + WebSocket to the Chronicle backend.

## Architecture

```
module.mjs (entry point)
  └─ SyncManager (sync-manager.mjs)
       ├─ ChronicleAPI (api-client.mjs)    ← REST + WebSocket client
       ├─ JournalSync (journal-sync.mjs)   ← Entity ↔ JournalEntry
       ├─ MapSync (map-sync.mjs)           ← Map + markers/drawings/tokens/fog/layers ↔ JournalEntry (image page) with overlay
       ├─ CalendarSync (calendar-sync.mjs) ← Calendar events/date ↔ Calendaria/SimpleCalendar
       ├─ ShopWidget (shop-widget.mjs)     ← Shop entity inventory in Foundry UI
       ├─ NoteSync (note-sync.mjs)        ← Chronicle Notes ↔ JournalEntry
       ├─ ItemSync (item-sync.mjs)        ← Actor inventory ↔ Chronicle relations
       ├─ SyncDashboard (sync-dashboard.mjs) ← 11-tab ApplicationV2 status/management UI
       └─ ActorSync (actor-sync.mjs)       ← Character entity ↔ Actor
            └─ generic-adapter.mjs          ← API-driven adapter (all systems)

scripts/update-info.mjs                    ← Diagnostic dialog for the Chronicle-served install/update flow
chronicle-package.json (repo root)          ← Serving descriptor consumed by Chronicle's `foundry_vtt` sub-plugin
tools/check-package-descriptor.mjs          ← CI script that validates the descriptor against module.json
```

### Data Flow

1. **GM starts Foundry** → `module.mjs` initializes SyncManager
2. **SyncManager.start()** → validates config, initializes modules, connects WS
3. **WebSocket connects** → performs initial sync (pulls mappings since `lastSyncTime`)
4. **Live updates** → WS messages routed to modules; REST for CRUD operations
5. **Bidirectional** → Foundry hooks detect local changes, push to Chronicle via REST

### Sync Guard

All sync modules use a `_syncing` boolean flag to prevent infinite loops:
- Set `true` before applying a remote change to Foundry
- Foundry's change hooks fire but see `_syncing === true` → skip pushing back
- Reset to `false` after the change is applied

### Real-time calendar date-push guard

`GET /calendar/date` carries `tracks_real_time` (`GET /calendar` never does).
When true, Chronicle's calendar is wall-clock authoritative, so the module
treats dates as **read-only**: it pauses only its own `PUT /calendar/date`
pushes (pull and event sync are untouched). `scripts/_realtime-date-guard.mjs`
centralizes this so `calendar-sync.mjs`'s three hook-triggered pushes and
`sync-dashboard.mjs`'s manual push button share one implementation: a
fetch-before-push `GET` re-run on every push attempt (never trust a cached
value — the round trip is cheap and self-heals a mid-session flag flip), plus
treating a `PUT` 422 as the same condition, never a retryable sync error. The
one-time-per-session GM notice is a module-level singleton shared by both
files. Tested at `tools/test-realtime-date-signal.mjs`.

### Applied-date confirmation

Chronicle's sync chip distinguishes what Foundry *saw* (the `GET
/calendar/date` beacon) from what it actually *applied* locally.
`calendar-sync.mjs`'s `_setLocalDate` — the single choke point both apply
paths (the poll path and the WebSocket `calendar.date.advanced` path) funnel
through — returns a `Promise<boolean>` reporting whether a real local
calendar-module setter ran without throwing ("no throw" is the strongest
available success signal; neither calendar module returns one). Both call
sites POST `scripts/_applied-date-confirm.mjs`'s `confirmAppliedDate(api,
{year, month, day})` → `POST /calendar/date/confirm`, only when
`_setLocalDate` resolves `true`. A 404/405 (older Chronicle without the
endpoint) is tolerated via a session-singleton debug-log-once. Tested at
`tools/test-applied-date-confirm.mjs`.

### Sync wire fixes

`sync-manager.mjs`'s `_onSyncStatus` reads `msg.status ?? msg.payload?.status`
(client emit shapes vary: `sync.status` is unwrapped, `sync.retryComplete` is
`{payload}`) so initial sync and reconnect resync actually fire. The
SimpleCalendar path shares the same structure-mismatch guard as Calendaria via
`_readActiveSimpleCalendarStructure()` / `_readActiveFoundryStructure()`, and
fails open on an unreadable structure. The dashboard's sync badge is a pure
four-state classifier (`scripts/_calendar-sync-state.mjs`:
`in-sync` · `date-drift` · `incompatible-structures` · `paused`). Visibility
toggles go through `POST /entities/:id/reveal`, not a bare `PUT` (which 400s
without a name). Item relations use the flat `DELETE|PUT /relations/:relationId`
paths with snake_case bodies. Pinned by `tools/test-sync-wire-fix.mjs` and
`tools/test-calendar-sync-state.mjs`.

Calendaria back-catalog note mappings are stored per-GM-user, not per-world, so
a second GM account duplicates notes. Open work: #95.

## Files

| File | Purpose |
|------|---------|
| `module.json` | Module manifest. Foundry v12-v13 compatible. Optional deps: Calendaria, Monk's Enhanced Journal |
| `scripts/module.mjs` | Entry point. Registers settings on `init`, starts SyncManager on `ready` (GM only) |
| `scripts/settings.mjs` | 9 world-scoped settings (apiUrl, campaignId, syncEnabled, syncJournals, syncMaps, syncCalendar, syncNotes, syncCharacters. 12 internal: lastSyncTime, detectedSystem, userMappings, syncExclusions, syncDirections, syncPermissions, defaultOwnership, dmOnlyHidden, conflictResolution, autoSync, excludedTags, excludedNamePattern |
| `scripts/constants.mjs` | Shared constants: FLAG_SCOPE, MODULE_ID |
| `scripts/api-client.mjs` | REST client (Bearer auth) + WebSocket (auto-reconnect, message queue, event emitter) |
| `scripts/sync-manager.mjs` | Orchestrator. Owns API client, routes WS messages, manages sync mapping lookups |
| `scripts/journal-sync.mjs` | Entity ↔ JournalEntry bidirectional sync. Monk's Enhanced Journal support |
| `scripts/map-sync.mjs` | Chronicle map sync. Materializes maps as JournalEntries (image page) inside an auto-created "Chronicle Maps" folder; renders markers/drawings/tokens/fog/layers as overlays inside `MapViewerSheet` rather than as Scene objects. Idempotent by `mapId` page flag. Visibility filtering enforced at materialization (GM) + render (defense-in-depth). |
| `scripts/map-viewer.mjs` | ApplicationV2 sheet (`MapViewerSheet`) rendering Chronicle map image with SVG overlay for markers, drawings, tokens, and fog. Registered as a sheet for image-type pages; selected per-page via `flags.core.sheetClass`. |
| `scripts/update-info.mjs` | "Update Source" diagnostic dialog (ApplicationV2). Reads the install-time manifest URL Foundry stored for this module, classifies it (chronicle/github/unknown), and provides a manual "Check Chronicle for updates" button. Surfaced via `settings.registerMenu` in `settings.mjs`. |
| `scripts/sync-calendar.mjs` | "Sync Calendar" editor (ApplicationV2, GM-only). 3-pane view of the active Calendaria calendar — left rail (year nav + structure summary + validation panel), center (year-overview grid OR month day-grid + moon strip), right (selected-day inspector OR note form). Writable day inspector (create/edit/delete notes via `CALENDARIA.api.createNote/updateNote/deleteNote`) with drag-select multi-day event creation; moon-phase strip across the month view (click-to-day, multi-moon convergence detection); a visual recurrence builder preset row in the note form (writes conditionTrees via `updateNote`); an empty-state Calendaria-import flow that POSTs to Chronicle's `POST /calendar` (degrades gracefully when the endpoint isn't deployed). Weather and structure editing are not yet implemented. Writes go through `CALENDARIA.api` (events) and the Chronicle api-client (`game.modules.get('chronicle-sync').api.getAPI()`, calendar import). Hooks throttled at 250 ms; in-flight form state survives re-renders. |
| `scripts/sync-calendar-validation.mjs` | Pure-function validation rule engine for the Sync Calendar editor. Each rule inspects the active calendar and returns either `null` or a finding (`{severity, code, message, fix_hint?, focus_target?}`). Unit-tested at `tools/test-sync-calendar-validation.mjs`. `SCHEMA_VERSION` stamps persisted user-flag state for forward-compat. |
| `scripts/sync-calendar-note-form.mjs` | Pure form ↔ Calendaria API translation for the Sync Calendar editor's note form. `formFromNote(note)` extracts form values; `noteOptionsFromForm(form)` produces the options object accepted by `CALENDARIA.api.createNote`/`updateNote`. `validateForm(form)` returns structural error keys for inline rendering. `VISIBILITY` + `DISPLAY_STYLE` constants mirror Calendaria's documented enums. Round-trips `conditionTree` (recurrence) between Calendaria notes and the form. Unit-tested at `tools/test-sync-calendar-note-form.mjs`; kept as pure functions outside the Application class so form logic is testable without a Foundry runtime. |
| `scripts/sync-calendar-moon-strip.mjs` | Pure moon-strip view-model builder. `buildMoonStripData({moons, year, monthOrdinal, daysInMonth, getPosition})` produces per-moon, per-day phase position arrays; `dayFromStripClick(x, width, days)` translates a CSS-grid click to a 1-indexed day; `findConvergenceDays(strip)` flags days where ≥2 deterministic moons are simultaneously full. Randomized-mode moons pass through verbatim (no synthesized noise). Unit-tested at `tools/test-sync-calendar-moon-strip.mjs` against the three fixture calendars. |
| `scripts/sync-calendar-condition-builder.mjs` | Pure conditionTree (recurrence) preset composer + validator. `treeFromPreset(presetId, params)` produces a Calendaria-compatible conditionTree for the common patterns (every weekday, every Nth of month, every full/new moon of X, every Nth weekday in season Y, every season-day, every cycle stage). `validateTree(tree)` enforces Calendaria's root-must-be-group rule and flags scope leaks beyond the supported patterns. `treeToSummary(tree)` produces a human-readable one-liner. Unit-tested at `tools/test-sync-calendar-condition-builder.mjs`. |
| `scripts/sync-calendar-diagnostics.mjs` | Pure builder `buildCalendarDiagnostics(input)` → Markdown report (versions, structure, all validation findings, selected-day notes/moons/weather, CALENDARIA.api capability probe, recent sync errors, settings). Backs the editor's **Copy diagnostics** button and the dashboard's **Copy Calendar Diagnostics** button (Status tab). Unit-tested at `tools/test-sync-calendar-diagnostics.mjs`. |
| `scripts/sync-calendar-import-from-calendaria.mjs` | Pure Calendaria → Chronicle calendar-create transform. `transformCalendariaCalendar(cal)` produces a `{payload, summary}` pair the Sync Calendar editor's empty-state flow POSTs to `POST /api/v1/campaigns/:cid/calendar`. Maps name/dates/months/weekdays/seasons/moons/eras; skips cycles/festivals/weather zones (surfaced in `summary.skipped`). `buildCalendarPreflightSummary(cal)` produces the one-line summary shown next to each import button. Wire shape pinned at `IMPORT_WIRE_VERSION: 1`. Unit-tested at `tools/test-sync-calendar-import-from-calendaria.mjs` against the three fixture calendars. |
| `chronicle-package.json` | Repo-root serving descriptor (schema v1). Chronicle's `foundry_vtt` sub-plugin reads this from the extracted release zip via `PostInstallHook` to learn how to serve this package: which fields to rewrite, what URL templates to emit, whether per-campaign signed tokens are required. Source of truth for the install/update flow's URL shape. |
| `tools/check-package-descriptor.mjs` | CI script. Validates `chronicle-package.json` against `module.json` (id match, file paths resolve, endpoint templates carry required placeholders). Run by `.github/workflows/check-descriptor.yml` on every push + PR. |
| `tools/test-lang-expand.mjs` | CI guard for `lang/en.json`. Simulates Foundry's `expandObject` and fails loud on any string-vs-object collision or duplicate-after-expansion path that would cause Foundry to reject the whole lang file at boot. See F-LANG-1 footgun below. |
| `tools/test-template-roots.mjs` | CI guard for ApplicationV2 PARTS templates. Discovers every `PARTS.*.template` binding in `scripts/*.mjs`, statically analyzes each referenced `.hbs` (recursing through every `{{#if/unless/each/with}}` branch), and asserts max-root-count == 1 — Foundry's `_parsePartHTML` rejects multi-root parts at render time. See F-PR3-4 footgun below. |
| `tools/test-sync-dashboard-calendar-diagnostics.mjs` | Static source pin + pure-function tests for the dashboard's Copy Calendar Diagnostics button. Asserts the action is registered in DEFAULT_OPTIONS, the import from sync-calendar-diagnostics.mjs is present, the template has the button and feedback element, and buildCalendarDiagnostics produces a valid report from dashboard-shaped input. Run: `node --test tools/test-sync-dashboard-calendar-diagnostics.mjs`. |
| `tools/test-actor-sync-pc-claim.mjs` | Structural + behavioral tests for Player Character Claiming addon-awareness. Verifies SyncManager._fetchAddonState / isPlayerClaimingEnabled, ActorSync._resolvePcSubTypeId / _maybeShowClaimingHint, entity_type_id selection ternary, and the _claimHintShown once-per-session guard. Run: `node --test tools/test-actor-sync-pc-claim.mjs`. |
| `scripts/calendar-sync.mjs` | Adapter pattern for Calendaria and SimpleCalendar. 0-indexed↔1-indexed conversion |
| `scripts/_realtime-date-guard.mjs` | Pure/session-singleton helper gating `PUT /calendar/date` when Chronicle's `tracks_real_time` is set. `shouldSkipDatePush(api)` does the fetch-before-push `GET /calendar/date` check; `isRealTimeRejection(err)` classifies a 422 backstop; `notifyRealTimePushPaused()` shows the once-per-session GM notice. Shared by `calendar-sync.mjs` and `sync-dashboard.mjs`. Unit-tested at `tools/test-realtime-date-signal.mjs`. |
| `scripts/_applied-date-confirm.mjs` | `confirmAppliedDate(api, {year,month,day})` POSTs `/calendar/date/confirm` (optional endpoint, Chronicle ≥ applied-beacon release) after `calendar-sync.mjs`'s `_setLocalDate` reports a real local apply. `isConfirmNotSupported(err)` classifies a 404/405 (older Chronicle); `notifyConfirmNotSupportedOnce()` debug-logs that tolerance once per session. Never throws — a confirm failure is swallowed so it can't surface as a sync error. Unit-tested at `tools/test-applied-date-confirm.mjs`. |
| `scripts/_calendar-sync-state.mjs` | Pure `classifyCalendarSyncState(input)` mapping the dashboard's calendar inputs to one of four honest states — `in-sync` / `date-drift` (w/ direction) / `incompatible-structures` (w/ counts) / `paused`. Priority: module-paused > dashboard-detected structure mismatch > date drift > in-sync. Fails open (never reports incompatible on a missing structure read). Unit-tested at `tools/test-calendar-sync-state.mjs`. |
| `scripts/shop-widget.mjs` | Shop window (Application v1). Context menu, drag-to-character-sheet, real-time updates |
| `scripts/actor-sync.mjs` | Actor ↔ character entity bidirectional sync. System adapter loading, hook registration. Addon detection via `isPlayerClaimingEnabled()` (SyncManager) for Player Character Claiming; PC sub-type routing (`_resolvePcSubTypeId` matches `preset_category=player_character` or `slug=player-character`), `owner_user_id` gated on addon state, `_maybeShowClaimingHint()` surfaces a one-time advisory when the addon is off but player-owned actors exist. |
| `scripts/item-sync.mjs` | Actor inventory ↔ Chronicle "Has Item" relations. System data sync (quantity, equipped) via relation metadata |
| `scripts/note-sync.mjs` | Chronicle Notes ↔ JournalEntries in "Chronicle Notes" folder tree. Case normalization (camelCase ↔ snake_case). Sharing → ownership mapping |
| `scripts/adapters/generic-adapter.mjs` | API-driven adapter: fetches field defs from `/systems/:id/character-fields` API, auto-generates mappings from `foundry_path` annotations in system manifests. Works for all installed systems |
| `scripts/sync-dashboard.mjs` | 11-tab ApplicationV2 dashboard (Overview, Config, Entities, Shops, Maps, Characters, Calendar, Issues, **Members**, Notes, Status). Pull/push actions, health metrics, error log, search/filter. **Members tab**: per-member Foundry-user mapping dropdown + matched/UNMATCHED badge, wired to `setUserMappings`; row view-model in `_member-mapping.mjs`. Status tab surfaces a **Copy Calendar Diagnostics** button and a **DM-only-hidden counter** (private entities that synced but landed hidden from players). |
| `scripts/_member-mapping.mjs` | Pure view-model `buildMemberRows({members, mappings, foundryUsers, keyOf})` for the dashboard Members tab — one row per Chronicle member with current mapping + matched/UNMATCHED badge (stale mappings → UNMATCHED). No Foundry globals; unit-tested at `tools/test-permission-sync.mjs`. |
| `templates/sync-dashboard.hbs` | Handlebars template for the sync dashboard tabs and content |
| `templates/shop-window.hbs` | Handlebars template for shop inventory display |
| `styles/chronicle-sync.css` | Status indicator + shop window styles |
| `styles/sync-calendar-pr3.css` | Sidecar stylesheet: moon-strip grid, recurrence section, empty-state import banner. Loaded after `chronicle-sync.css` via `module.json#/styles` so the cascade gives it the higher-specificity tail. Kept in a sidecar to avoid a single 50KB+ source file. |
| `lang/en.json` | English localization for all UI strings |
| `TESTING.md` | E2E testing checklist (journals, maps, calendar, shops, characters) |
| `CALENDARIA-INTEGRATION.md` | Calendaria data-model reference (notes = JournalEntries, festival seeding, flags, hooks, API), verified against upstream source. Read before touching any `createJournalEntry` handler. See F-SYNC-1. |

## API Endpoints Used

See **API-CONTRACT.md** for the full canonical API contract with request/response
schemas, WebSocket protocol, and authentication details.

The table below summarizes which module uses which API area:

| Feature | Endpoints | Module |
|---------|-----------|--------|
| Auth | Bearer token header (REST), `?token=` query (WS) | `api-client.mjs` |
| Systems | `GET /systems`, `GET /systems/:id/character-fields`, `GET /systems/:id/item-fields` | `sync-manager.mjs`, `generic-adapter.mjs`, `item-sync.mjs` |
| Entities | `GET/POST/PUT/DELETE /entities/*`, `PUT /entities/:id/fields`, `POST /entities/:id/reveal` | `journal-sync.mjs`, `actor-sync.mjs` |
| Entity Types | `GET /entity-types` | `actor-sync.mjs`, `journal-sync.mjs` |
| Entity Permissions | `GET/PUT /entities/:id/permissions` | `journal-sync.mjs` |
| Relations | `GET /entities/:id/relations`, `POST/PUT/DELETE .../relations/*` | `item-sync.mjs`, `shop-widget.mjs` |
| Maps | `GET /maps` | `map-sync.mjs` |
| Map Markers | `GET/POST/PUT/DELETE /maps/:id/markers/*` | `map-sync.mjs` |
| Calendar | `GET /calendar`, `GET/PUT /calendar/date` (`tracks_real_time` gates push), `POST /calendar/date/confirm` (optional, older Chronicle lacks it), `POST /calendar/advance`, `POST /calendar/advance-time` | `calendar-sync.mjs`, `_realtime-date-guard.mjs`, `_applied-date-confirm.mjs` |
| Calendar Events | `GET/POST/PUT/DELETE /calendar/events/*` | `calendar-sync.mjs` |
| Calendar Sub-Resources | `GET/PUT /calendar/{seasons,moons,eras,event-categories,cycles,festivals,weather}` | Not yet used |
| Calendar Structure | `GET /calendar/structure`, `PUT /calendar/{settings,months,weekdays}`, `GET /calendar/export`, `POST /calendar/import` | Not yet used |
| Notes | `GET/POST/PUT/DELETE /notes/*` (camelCase responses, snake_case requests) | `note-sync.mjs` |
| Sync Mappings | `GET/POST/DELETE /sync/mappings`, `GET /sync/lookup`, `GET /sync/pull`, `POST /sync` | `sync-manager.mjs`, all sync modules |
| Media | `POST /media/upload`, `GET/DELETE /media/:id` | `api-client.mjs` |
| Members | `GET /members` | `sync-manager.mjs` |
| Addons | `GET /addons` | `sync-manager.mjs` |

## WebSocket Message Types

See API-CONTRACT.md for the full WebSocket protocol. Summary of handling status:

**Handled:**
- `entity.created/updated/deleted` → JournalSync
- `entity_type.created/updated/deleted` → JournalSync (folder updates)
- `marker.created/updated/deleted` → MapSync
- `calendar.event.created/updated/deleted` → CalendarSync
- `calendar.date.advanced` → CalendarSync
- `note.created/updated/deleted` → NoteSync
- `relation.created/deleted/metadata_updated` → ItemSync
- `sync.status` → SyncManager `_onSyncStatus` (fires the one-time initial sync
  on first `connected`; emitted unwrapped as `{status}`, read as
  `msg.status ?? msg.payload?.status`)

**Calendar blackout** — see CLAUDE.md → "Calendar blackout" and API-CONTRACT.md
→ "CALENDAR BLACKOUT" for the single source of truth. Chronicle's calendar
plugin is deleted pending the V5 rebuild, all calendar routes answer `503
calendar_rebuilding`, and every `calendar.*` WebSocket type is dormant.

**Not yet handled (available per contract):**
- `sync.error` — Synchronization error
- `sync.conflict` — Data conflict detected

Every other `calendar.*` type hits a `default:` branch that logs one
`console.debug` line per type per session, so an unhandled broadcast still
leaves a trace.

### Calendar sub-resources

**Display-level and non-destructive.** No sub-resource handler writes into a
Foundry document. Three specific abstentions, each deliberate:

1. **No structure auto-apply on `calendar.structure.updated`.** Rewriting the
   Foundry calendar's months/weekdays from a broadcast is the most destructive
   write available to this module: Calendaria stores notes against month/day
   coordinates, so re-shaping the calendar silently re-dates every note in the
   world. The handler re-compares and badges instead. Non-destructive
   auto-merge is a later arc (calendar-remodel requirements §1 item 10).
2. **No auto-created celestial note on `calendar.worldstate.changed`.** The
   payload carries no celestial detail and no stable event id, so a
   re-broadcast or reconnect would create duplicate notes with nothing to
   dedupe on. Announcing what the payload contains is honest; inventing a
   "Meteor Shower" note from a mood tint is not.
3. **No speculative Calendaria weather setter.** Calendaria publishes weather
   READS; no shipped build exposes a documented setter. The module probes
   `setWeather` / `setCurrentWeather` / `setWeatherForDate` and degrades to a
   GM chat line, with the probe result mirrored into the diagnostics bundle so
   a build that does expose one shows up in an operator's bug report.

**Decision: announcements are GM whispers, never public chat.** Chronicle gates
dm_only traffic server-side (`Message.RequiresDM`, `internal/websocket/hub.go`),
but "cleared for the GM" is not "cleared for the table" — weather zones and
world state routinely encode what the DM is holding back. Re-broadcasting would
launder a server-side permission decision into a player-visible one, the leak
class cordinator#32 hardened against. `CalendarSync._announceToGM` whispers to
the GM user ids and is the module's only chat surface.

**Decision: structure signals are processed while paused.** `onMessage` routes
them ahead of the `_calendarSyncDisabled` guard, because fixing the calendar in
Chronicle — the documented remedy for a mismatch pause — is precisely what emits
the broadcast. Behind the guard, a pause would survive its own cause until a
world reload. This is also the only path that can clear a mismatch pause.

**Chronicle-side gap: `calendar.worldstate.changed` never reaches the wire** —
see API-CONTRACT.md → "Gap: `calendar.worldstate.changed` never reaches the
wire" for the current state. The module's handler is wired and tested; it
lights up once the gap closes on the Chronicle side.

## Security posture

This section consolidates the security invariants this module relies on.

### For humans

Chronicle Sync trusts a single Chronicle deployment that the operator configures. The trust posture is **targeted sanitization at the highest-risk surfaces** — we do not validate every Chronicle response field, but we hard-gate the three surfaces where a compromised Chronicle could harm a Foundry client: stored HTML, `<img src>` URLs, and `innerHTML` interpolation in the dashboard. The descriptor schema gets both a CI check and a runtime re-check so silent drift surfaces immediately.

Two auth surfaces exist and **never cross**: the per-campaign `apiKey` for `/api/v1/...` traffic (REST + WS), and the install-URL signed token for the `/foundry-vtt/module.json` + zip download endpoints (manifest fetch only). A past wire-contract drift was caught at this seam — regression-pin tests keep it from recurring.

### Auth surface separation

| Surface | Auth carrier | URL shape | Audience |
|---|---|---|---|
| REST API (sync) | `Authorization: Bearer ${apiKey}` header | `${apiUrl}/api/v1/campaigns/${campaignId}/...` | All sync modules, dashboard, wizard |
| WebSocket (sync) | `?token=${apiKey}` query param | `wss://...${apiUrl host}/ws?token=...&client=foundry-module` | `api-client.mjs::_doConnect` only |
| Module manifest fetch | Install-URL signed token in URL | `${apiUrl}/api/v1/campaigns/${campaignId}/foundry-vtt/module.json?token=...` | Foundry's update-check flow (not us — Foundry's installer reads the URL) |
| Module zip download | Install-URL signed token in URL | `${apiUrl}/api/v1/campaigns/${campaignId}/foundry-vtt/module.zip?token=...` | Foundry's installer |

The lesson: a refactor on either side that "consolidates" the two tokens (e.g., uses `apiKey` for manifest fetch, or install-URL token for `/api/v1/`) breaks both. Reasoning: the install-URL token is signed by Chronicle's per-campaign signing key and includes campaign + expiration claims that `/api/v1/` doesn't accept; the `apiKey` is a long-lived bearer token that `/foundry-vtt/module.json` won't accept. They're different secrets with different validation paths.

**Pinned by:** `tools/test-api-client-security.mjs` — asserts every REST method sends Bearer, asserts WS connect uses `?token=` and never Bearer, asserts no `/syncapi/` substring anywhere.

### Trust posture

Chronicle is operator-controlled but treated as a hostile-content-broker for three classes of data:

| Class | What we trust | What we validate | Where |
|---|---|---|---|
| HTML for `JournalEntry.text.content` | Chronicle's bluemonday UGCPolicy at write | Pre-sanitize via `TextEditor.cleanHTML` at ingress (defense-in-depth) | `scripts/_html-sanitizer.mjs` |
| Image URLs from map sub-resources | Operator-configured `apiUrl` host | Reject full-URL `image_url` / token `image` whose hostname doesn't match `apiUrl` | `scripts/_url-validation.mjs` |
| User-visible DOM interpolation | Nothing — we never trust Chronicle strings as HTML in our own UI | DOM construction via `replaceChildren` + `createTextNode` (no `innerHTML` interpolation of Chronicle data) | `scripts/sync-dashboard.mjs::_renderTestResults` |
| `campaignId` setting value | Operator typed it correctly | UUID v4 shape (8-4-4-4-12 hex) before any URL interpolation | `scripts/_settings-validation.mjs` |
| Outbound error log entries | Nothing (Chronicle's response body may echo our request) | Scrub `Bearer <token>` and `?token=<value>` before storing in the recent-errors ring buffer | `scripts/api-client.mjs::_scrubAuthHeaders` |

What we DON'T validate:
- Field shapes on Chronicle responses (`entity.name: string`, `entity.tags: array`). Blanket validation was considered and rejected; implementation cost was high and the operator-controlled-Chronicle threat model didn't justify it.
- The contents of WebSocket payloads beyond the `type` allowlist — once `type` matches a known prefix, the handler trusts the payload shape.

### Sanitization layers

Three-layer defense on Chronicle HTML reaching a player's browser:

1. **Chronicle server-side (primary).** bluemonday UGCPolicy at the service layer, across every plugin that stores rich text. Runs on write — `EntryHTML` is sanitized before it's stored in Chronicle's database.
2. **Foundry-side ingress (defense-in-depth).** `_sanitizeIncomingHTML(html)` wraps Foundry's `TextEditor.cleanHTML` and runs at six ingestion sites in `journal-sync.mjs` + `note-sync.mjs`. Catches a Chronicle regression that bypasses layer 1, an old DB entry that pre-dates a rule strengthening, or a hand-edit that skips the service layer. Operator escape: `skipIncomingSanitization` setting (default off = sanitization on).
3. **Foundry render-time.** `TextEditor.enrichHTML` runs on every render of a `JournalEntry.text.content`. This is Foundry's stock defense and survives even if layers 1 + 2 both fail (e.g., a hand-edited page in a release zip).

**Pinned by:** `tools/test-html-sanitizer.mjs` — static-source scan asserts every `entity.entry_html` / `note.entry_html` ingestion site is wrapped by `_sanitizeIncomingHTML`. A new ingestion path that bypasses the sanitizer fails CI.

### Descriptor schema integrity

`chronicle-package.json` is Chronicle's source of truth for how to serve this module: where the manifest lives inside the zip, what URL shape to emit, whether per-campaign signing is required. The Chronicle backend reads it via a `PostInstallHook`, falling back to hardcoded defaults if the file is absent or malformed — Chronicle prefers degraded-but-functional serving over a hard error.

Two validators run the same `validateDescriptor(descriptor, moduleJson)` function:
- **CI** (`tools/check-package-descriptor.mjs`): runs on every PR via `.github/workflows/check-descriptor.yml`. Adds an on-disk existence check for `package.moduleJsonPath` (CI-only concern).
- **Runtime** (`scripts/module.mjs::_runtimeValidateDescriptor`): GM-gated, called from the `ready` hook. Fetches the deployed descriptor + module.json, runs the same validator. Surfaces a sticky `ui.notifications.error` on drift; non-fatal.

This means a release zip with a malformed descriptor that bypassed CI (hand-edit, ad-hoc deployment) is caught on the GM's first world load, not after silent drift accumulates.

**Pinned by:** `tools/test-descriptor-validator.mjs` — static-source pin that both call sites import the shared `validateDescriptor`.

### Security footguns

| Finding | Fix | Pin |
|---|---|---|
| `sync-dashboard.mjs::_renderTestResults` interpolated step-result text into `innerHTML` — a Chronicle-supplied test message could carry an XSS payload | DOM construction via `replaceChildren` + `createElement` + `createTextNode` | `tools/test-sync-dashboard-xss.mjs` |
| Map `image_url` / token `image` accepted any full URL — a tampered Chronicle could redirect fetches to an attacker host, leaking IP + cookies | `_isAllowedImageHost(url, apiUrl)` host-allowlist at 5 callsites in `map-sync.mjs` + `map-viewer.mjs`; rejection drops to a blank `<img>` + console warning | `tools/test-url-validation.mjs` |
| `entity.entry_html` / `note.entry_html` stored raw into `JournalEntry.text.content` — relied solely on Foundry's render-time sanitizer | `_sanitizeIncomingHTML(html)` wrapping `TextEditor.cleanHTML` at 6 ingestion sites in `journal-sync.mjs` + `note-sync.mjs`; operator escape `skipIncomingSanitization` | `tools/test-html-sanitizer.mjs` |
| `campaignId` setting interpolated into REST URLs without shape validation — a typo or malicious value could break out of the URL path | `_validateCampaignIdOrThrow(id)` enforces UUID v4 shape at `api-client.fetch` + `uploadMedia` entry; the WS path doesn't carry campaignId so it's skipped there | `tools/test-campaign-id-validation.mjs` |
| `_logError` truncated and stored Chronicle response bodies that may echo `Bearer <token>` or `?token=<value>` — secrets surfaced in the dashboard's recent-errors ring | `_scrubAuthHeaders(text)` regex-replaces both shapes with `[redacted]` before truncation/storage | `tools/test-api-client-secrets.mjs` |

### Auth-surface quick reference

- API key stored as a Foundry **client** setting (this browser only — a world-scoped setting is readable by every connected player via `game.settings.get`), masked as a password input via `Hooks.on('renderSettingsConfig', ...)` in `settings.mjs`; `migrateApiKeyToClientScope()` moves a legacy world value across and deletes the world document
- REST uses `Authorization: Bearer <key>` header — never `?token=` query
- WebSocket uses `?token=<key>` query param (browsers don't allow setting headers on the WS upgrade) — never `Authorization`
- Module-manifest fetch uses install-URL signed token in the URL — a different secret from `apiKey`
- Per-key rate limiting enforced server-side (60 req/min default)
- Campaign-scoped: `apiKey` can only access its bound campaign
- Device fingerprint binding: optional one-time device lock (Chronicle-side)

### Cross-references

- **Chronicle conventions:** `chronicle/.ai/conventions.md` security section — the symmetric documentation on the server side
- **Tenets:** the binding tenets referenced in "Working with this project" below (T-B1 security first, T-B4 docs for humans and AI alike)

## Known API Field Discrepancies

The API contract (API-CONTRACT.md) defines the canonical field names. The code
currently uses some legacy/alternate names that the API also accepts. These
should be verified against the live API and aligned if possible:

| Area | Contract Field | Code Uses | Files |
|------|---------------|-----------|-------|
| Entity content (GET) | `content` | `entry_html` | `journal-sync.mjs` |
| Entity content (POST) | `content` | `entry` | `journal-sync.mjs` |
| Entity visibility (GET) | `visibility: "public"` | `is_private: true/false` + `visibility` | `journal-sync.mjs`, `actor-sync.mjs` |
| Sync mapping POST | `type`, `foundry_id` | `chronicle_type`, `external_id`, `external_system`, `sync_direction` | All sync modules |
| Sync lookup query | `?foundry_id=X&type=Y` | `?external_system=foundry&external_id=X` | `sync-manager.mjs` |
| Relations (READ / WS payload) | — | `sourceEntityId`, `targetEntityId`, `relationType` (camelCase) | `item-sync.mjs`, `shop-widget.mjs` |
| Relations (WRITE body) | `target_entity_id`, `relation_type`, `reverse_relation_type` (snake_case) | snake_case | `item-sync.mjs` |
| Calendar events (POST) | `color`, `icon`, `all_day`, `recurrence_*`, `start_hour`, `start_minute` | Not sent (uses simplified fields) | `calendar-sync.mjs` |

**Note:** These may represent legitimate API aliases (the API accepts both forms)
or WS vs REST payload differences. Verify with the Chronicle dev before changing.
**Relations are asymmetric on purpose:** Chronicle SERIALIZES relations camelCase
(the read/WS shape — `internal/widgets/relations/model.go`) but BINDS writes
snake_case (`apiCreateRelationRequest`/`apiUpdateRelationRequest`,
`syncapi/api_handler.go`). The module read path was always correct; the write
path was camelCase and 400'd until it was fixed. Relation routes are also
FLAT (`/relations/:relationId`), not nested under `/entities/:id`.

## Known Limitations

- **Map drawings/tokens/fog/layers**: read-only — rendered as SVG overlays in
  `MapViewerSheet`, not editable from Foundry. Only markers are bidirectional.
- **SimpleCalendar events**: Limited — events managed as journal notes, no CRUD hooks
  (the structure-mismatch guard covers the SimpleCalendar path too)
- **Shop icon field**: Always returns null (`shop-widget.mjs:146`)
- **Single scene**: Only the active Foundry scene syncs (no multi-scene)
- **GM only**: Full sync runs only for GM users; players get passive updates via Foundry
- **Character sync: limited fields back from PF2e**: Only HP and name sync from Chronicle to PF2e actors (most values are derived from items/rules)
- **Character sync: custom system requirements**: Custom game systems must include `foundry_path` annotations on their character preset fields for bidirectional sync. Systems without annotations get no character sync (the generic adapter returns null). dnd5e and pf2e have hand-written adapters that handle edge cases.
- **System matching**: Fully API-driven. `_detectSystem()` queries `/systems` and matches by `foundry_system_id` from each system's manifest. Any installed system package with a `foundry_system_id` is automatically detected.

## Features

- **Journal sync.** Entity `entry_html` splits by `<h1>`/`<h2>` headings into
  Foundry pages (tracked via `chronicle-sync.pageMap`) and concatenates back to
  a single `entry` on the way to Chronicle. Ownership changes on
  `updateJournalEntry` push to Chronicle.
- **Permission mapping.** Chronicle `visibility: 'custom'` + `entity_permissions`
  maps to per-user Foundry ownership (`view`→`OBSERVER`, `edit`→`OWNER`) via
  `GET/PUT /entities/:eid/permissions`, and reverse-maps Foundry ownership
  edits back to Chronicle. Per-user push
  (`journal-sync.mjs::_buildPermissionGrants`) skips unmapped Foundry users
  rather than over-sharing, and surfaces them as a dashboard warning. The
  **Members tab** lists every Chronicle member with a Foundry-user dropdown
  (auto-matched by display name, `SyncManager.memberKey()` is the matching
  key) and an unmatched badge. `tag_grants` can't be expanded to Foundry users
  so they surface as a warning rather than silently dropping. The Status tab
  counts DM-only entities that synced but are hidden from players. Fail-closed
  throughout: any ambiguity resolves toward less player visibility.
- **System detection.** `SyncManager._detectSystem()` queries `GET /systems` on
  start, matches `game.system.id`, and gates the `syncCharacters` setting.
- **Actor sync.** `actor-sync.mjs` bidirectionally syncs Foundry Actors and
  Chronicle character entities via `createActor`/`updateActor`/`deleteActor`
  hooks and `entity.*` WS events. Deleting from Chronicle unlinks the Actor
  (data preserved); deleting from Foundry deletes the Chronicle entity.
- **Diagnostics.** The API client tracks success/error counts, uptime, and
  reconnect attempts; keeps a 50-entry error log; and retries failed writes on
  reconnect (max 3 tries, 50-item cap). Surfaced on the dashboard Status tab.
- **Generic system adapter.** `generic-adapter.mjs` fetches field definitions
  from `/systems/:id/character-fields` and auto-generates
  `toChronicleFields()`/`fromChronicleFields()` from `foundry_path`
  annotations, honoring `foundry_writable`. Any system package with
  `foundry_system_id` and `foundry_path` fields gets character sync with no
  custom code; systems without `foundry_path` get none.
- **NPC visibility.** Chronicle `is_private` ↔ Foundry `prototypeToken.hidden`
  syncs bidirectionally.
- **Inventory / armory.** `item-sync.mjs` syncs actor inventory to Chronicle
  "Has Item" relations (quantity, equipped status) via
  `createItem`/`updateItem`/`deleteItem` hooks; item fields load from
  `/systems/:id/item-fields`.
- **Shop widget.** `shop-widget.mjs` is an ApplicationV2 window with
  drag-to-character-sheet; a purchase creates a relation + inventory item and
  updates in real time over WS. Opens from a context menu on shop-type
  JournalEntries.
- **Notes sync.** `note-sync.mjs` syncs Chronicle Notes (a resource separate
  from entities, own API and folder tree) to a "Chronicle Notes" folder in
  Foundry, preserving folder hierarchy and mapping sharing to ownership
  (shared → `OBSERVER`, private → `NONE`).
- **Player Character Claiming.** When the Chronicle `player-character-claiming`
  addon is on (`SyncManager.isPcClaimingEnabled()`, from `GET /addons`),
  player-owned actors route to Chronicle's PC entity sub-type and include
  `owner_user_id` on create; with the addon off, actors get no auto-claim.
  `_isCharacterEntity()` accepts both the parent character type and the PC
  sub-type. The dashboard Characters tab shows a hint to enable the addon when
  there are player-owned PC actors but the addon is off.
- **Conflict handling.** `api-client.mjs`'s `ConflictError` (409, stale
  `expected_updated_at`) is handled by JournalSync and ActorSync per the
  `conflictResolution` setting: `chronicle` (re-pull), `foundry` (force push),
  or `newest` (compare timestamps).

Tests: `tools/test-permission-sync.mjs`, `tools/test-build-ownership.mjs`,
`tools/test-pc-claiming.mjs`, and the per-module suites listed in `TESTING.md`.

## Chronicle Integration — Install & Updates

Foundry installs and updates this module from **Chronicle**, not from GitHub.
Chronicle's `foundry_vtt` sub-plugin serves each campaign a signed,
per-campaign install URL pinned to whichever version the campaign owner picked.

Chronicle itself gets each version from **this repo's GitHub releases**: its
packages plugin imports them ("Check for updates" in Admin → Packages) and
falls back to GitHub's source zip when a release has no attached asset. So
releasing means publishing a GitHub release with a version tag such as
`0.1.30`; nothing is uploaded to Chronicle by hand.

### Install flow

1. Campaign owner pins a version (or leaves auto-latest) in Chronicle's
   campaign settings → **Foundry VTT** disclosure.
2. Chronicle generates a signed per-campaign manifest URL:
   `/api/v1/campaigns/{campaign_id}/foundry-vtt/module.json?token={signed}`.
3. Owner copies the URL; user pastes into Foundry's **Install Module** dialog.
4. Foundry GETs the URL — Chronicle responds with the resolved version's
   `module.json` with `manifest` + `download` fields rewritten to per-campaign
   Chronicle URLs (instead of the GitHub URLs in the source `module.json`).
5. Foundry follows `download` to fetch the zip; extracts; module installed.
6. Foundry stores the install-time manifest URL and uses it for every future
   update check — so updates always come from Chronicle.

### How Chronicle knows how to serve this module

`chronicle-package.json` at repo root is the **serving descriptor**, and it is
the **contract between this repo and Chronicle's `packages` plugin**. Every
Foundry-specific serving assumption Chronicle would otherwise hardcode lives
in this one file, so the same Chronicle code can serve this module or any
future Foundry module without changes on Chronicle's side.

Chronicle's `PostInstallHook` (added in C-FMC-5b) reads it from the extracted
release zip to learn:

- Which JSON file in the zip is the manifest (`package.moduleJsonPath`).
- Which fields to rewrite on serve (`serving.rewriteFields` — currently
  `manifest` + `download`).
- What URL shape to emit (`serving.manifestEndpoint`, `serving.downloadEndpoint`).
- Whether per-campaign signed tokens are required.

If the descriptor is absent or invalid in a particular release zip, Chronicle
falls back to hardcoded defaults equivalent to the current descriptor. The
descriptor is the "Foundry instructs Chronicle" surface — Foundry-specific
serving knowledge lives here, not hardcoded in Chronicle.

CI validates the descriptor on every push and PR via
`tools/check-package-descriptor.mjs` (run by
`.github/workflows/check-descriptor.yml`). Bumping
`module.json#/id` (rare) requires updating `chronicle-package.json#/package/id`
in the same commit, and CI enforces this.

For the full URL contract — request shapes, response shapes, error codes —
see `API-CONTRACT.md` → "Chronicle-served Module Distribution".

### Why `module.json`'s `manifest` and `download` fields still point at GitHub

The on-disk `module.json` keeps its GitHub URLs in `manifest` and `download`.
These are the **pre-rewrite** values; Chronicle overrides them on serve. They
serve two purposes:

1. If anyone obtains the source zip directly (not via Chronicle), they get
   sane, working defaults pointing at GitHub Releases.
2. Chronicle's `PackagesPlugin` uses the GitHub URLs to auto-pull new releases
   into its catalog.

Do **not** "fix" these to Chronicle URLs in the source `module.json`. The
descriptor's `serving.rewriteFields` array is what wires the rewrite at serve
time.

### Update Source diagnostic dialog

`scripts/update-info.mjs` provides a per-install diagnostic at
**Game Settings → Module Settings → Chronicle Sync → Update Source**:

- Surfaces the install-time manifest URL Foundry stored.
- Classifies it (chronicle / github / unknown) so the operator can confirm
  whether the install is wired up to Chronicle's per-campaign endpoint.
- Manual "Check Chronicle for updates" button hits the install-time URL
  directly so the operator can confirm reachability and inspect the response.

The Chronicle classifier regex is the locked URL shape
(`/api/v1/campaigns/.../foundry-vtt/module.json`). If Chronicle's URL shape
ever changes, update both `update-info.mjs` and `chronicle-package.json` in
the same commit.

#### Passive recovery probe on startup

`scripts/module.mjs`'s `ready` hook fires `surfaceManifestRecoveryIfNeeded()`
(GM-only, fire-and-forget) right after `syncManager.start()`. The helper
hits the install-time manifest URL once, reuses the same `categorize()`
classification the manual check uses, and if the result is `auth`
category (Chronicle 403 with `body.category: "auth"`, or any 401/403
without a Chronicle body) it surfaces a sticky `ui.notifications.error`
with Chronicle's `body.message` (or a localized fallback) instructing the
GM to reinstall from a fresh URL. Non-`auth` outcomes are intentionally
silent — they're either non-actionable from the GM's seat (`config`,
`internal`) or covered by the dialog when the GM opens it manually
(`network`, `parse`, `not_found`).

Why GM-only: only GMs can reinstall a module in Foundry. Surfacing the
prompt to players would be noise without recovery.

Why one-shot: the banner is sticky (`{permanent: true}`) so it persists
until the GM dismisses or fixes the install. Re-probing on every refresh
would either spam or require deduplication state with no real benefit.

#### How errors are classified

When the manual check fails, the dialog renders a 4-clause diagnostic
(*what we tried / what happened / likely cause / what to do*) tagged with
one of seven error categories — Chronicle's five server-side buckets plus
two Foundry-only fallback buckets for failures Chronicle never sees.

Categorization order (`scripts/update-info.mjs:104-110`):

1. **Trust Chronicle's `body.category`** when the response carries one of
   the five values in the wire-contract enum (`auth | config | not_found
   | validation | internal`). Chronicle classifies server-side; Foundry
   does not second-guess it. The five-value enum is pinned by cordinator
   `decisions/2026-05-17-error-catalog-wire-contract.md`.
2. **HTTP-status fallback** when `body.category` is absent or
   unrecognized: 401 / 403 → `auth`, 404 → `not_found`, 5xx → `internal`,
   anything else → `internal`. Covers proxy / CDN errors and Chronicle
   releases that add a new category before Foundry's enum is updated.
3. **Foundry-built fallback** for failures that never reach Chronicle:
   `network` (fetch threw) and `parse` (response wasn't JSON, or 200 OK
   was missing the `version` field). The precondition "no install-time
   URL recorded" reuses the `not_found` category with a client-built
   4-clause.

| Category     | Source                                  | Triggered by                                                                                | Icon (FontAwesome)         | CSS hue |
|--------------|-----------------------------------------|---------------------------------------------------------------------------------------------|----------------------------|---------|
| `auth`       | Chronicle (`body.category`) or HTTP 401/403 | `invalid_token`, or any 401/403 without a Chronicle body                                | `fa-key`                   | red     |
| `config`     | Chronicle (`body.category`)             | `no_package_registered`, `pinned_version_not_installed`, `token_not_initialized`, `no_version_available` | `fa-gear`                  | amber   |
| `not_found`  | Chronicle (`body.category`) or HTTP 404 | `campaign_not_found`, or any 404 without a Chronicle body, or "no install-time URL recorded" precondition | `fa-magnifying-glass`      | amber   |
| `validation` | Chronicle (`body.category`)             | `descriptor_invalid` (release zip's `chronicle-package.json` failed Chronicle's schema check) | `fa-triangle-exclamation`  | amber   |
| `internal`   | Chronicle (`body.category`) or HTTP 5xx | `module_json_missing`, the catch-all `ErrInternal` wildcard, or any 5xx without a Chronicle body | `fa-server`                | red     |
| `network`    | Foundry-side                            | `fetch` threw before reaching Chronicle (DNS, TLS, CORS, firewall)                          | `fa-plug-circle-xmark`     | slate   |
| `parse`      | Foundry-side                            | JSON parse failed, or 200 OK with no `version` field                                        | `fa-file-circle-question`  | slate   |

The category names are snake_case on the wire — e.g. `not_found` is
spelled with an underscore, never a hyphen. CSS class names
(`result-auth`, `result-config`, `result-not_found`, etc.) mirror the
category strings exactly so the classifier output drops straight into
the template.

There is intentionally **no** code-to-category lookup table in
`update-info.mjs`. The classifier trusts `body.category` because (a)
Chronicle already classifies server-side and ships an actionable
`body.message`, (b) duplicating the mapping in Foundry risks the two
sides drifting apart. The canonical list of `error` codes lives in `API-CONTRACT.md`
→ "Chronicle-served Module Distribution" → "Error code catalog", which
mirrors `error-catalog.json` at the pinned Chronicle URL.

### Token rotation and recovery

The campaign owner can rotate their per-campaign token from Chronicle's
Foundry VTT disclosure in campaign settings. Rotation is **immediate** —
the old token returns `403 { "error": "invalid_token", "category":
"auth", "message": "..." }` from the moment the new one is issued.
Implications:

- **Already-installed Foundry instances** will start failing update checks
  on their next probe. Foundry's native "Check for Update" UI surfaces this
  as a generic failure; the Update Source dialog surfaces it as the
  `auth` category with an actionable "reinstall from a fresh URL" message
  rendered verbatim from `body.message`.
- **There is no in-place repair** — we don't have a supported way to swap
  out the install-time URL Foundry stored. Recovery is: campaign owner
  shares the new install URL, user uninstalls + reinstalls the module via
  that URL, Foundry stores the new URL, update checks resume.
- **The pin survives rotation.** Rotating a token does not change which
  module version is pinned; only the credential changes.

### The release workflow

`.github/workflows/release.yml` produces a release zip that includes
`chronicle-package.json` alongside `module.json`. The workflow's tag-push
trigger is currently disabled (the operator drives releases manually via
`workflow_dispatch`); Chronicle's `PackagesPlugin` auto-polls GitHub releases
and pulls anything it finds, so the publishing cadence is whatever the
operator chooses.

## Foundry conventions and footguns

Pinned here so future PRs don't re-learn lessons that bit earlier ones.
Each entry: short name, when it bit, the rule.

### F-PR2-1: ApplicationV2 `actions` only delegates `click`

Pointer events (`pointerdown`/`pointerenter`/`pointerup`), keyboard events,
and any non-`click` interaction must be wired directly in `_onRender` via
`element.querySelectorAll(...).addEventListener(...)`. Adding the event
name to the `actions` map is silently dropped — there is no error. The
Sync Calendar editor's drag-select and moon-strip click both live in `_onRender`.

### F-PR2-2: Hook re-render preserves in-progress form state

Calendaria hooks (`dateTimeChange`, `noteCreated`, etc.) can fire mid-edit
and trigger an Application re-render. If the form's input values lived
only in the DOM they would be wiped. The pattern: write every input
keystroke to a class-side `_formData` (or equivalent) object via a
`#wireFormInputs` helper attached in `_onRender`; the next render reads
from `_formData`, not from DOM-side defaults. The 250 ms re-render
throttle in `#scheduleRerender` also helps.

### F-PR2-3: Test bootstrap for files that touch `foundry.applications.api`

Importing `scripts/calendar-sync.mjs` (or anything that transitively pulls
`settings.mjs` → `update-info.mjs` / `sync-calendar.mjs`) from a unit
test breaks with `ReferenceError: foundry is not defined` because those
files reference `foundry.applications.api` at module top level. Solution:
stub `globalThis.foundry`, `globalThis.game`, and `globalThis.Hooks`
before the dynamic `import()` call. See
`tools/test-calendar-sync-visibility.mjs` for the canonical stub block.

### F-PR2-4: i18n key collisions are silent

Foundry's i18n is a flat lookup with no namespace ownership. Two
`game.settings.registerMenu` calls cannot share an i18n key — Foundry
returns the first match silently. Convention: settings-menu entries
live under `CHRONICLE.Settings.<menuId>Menu.*`, never under
`CHRONICLE.Settings.<menuId>.*` (that namespace is the boolean toggle's).

### F-PR3-1: Foundry Handlebars helpers must be registered explicitly

Handlebars itself ships no `json` helper. Foundry registers a small core
set, but `eq`, `neq`, `lt`, `gt`, `json`, `timeAgo`, etc. are all
module-registered in `scripts/module.mjs`'s `init` hook. When adding a
template that needs a new helper, register it alongside the existing
ones — the `json` helper exists to stash preset params in `data-*`
attributes.

### F-PR3-2: Calendaria's `conditionTree` root must be a group

A `conditionTree` whose root is not `{type: 'group', mode, children}` is
silently ignored by Calendaria's matcher — the note never fires. The
operator sees no error. The pure validator in
`scripts/sync-calendar-condition-builder.mjs` catches this; the form's
save path blocks on `Errors.RootMustBeGroup`,
`Errors.EmptyGroup`, and `Errors.NotAnObject`.

### F-PR3-4: ApplicationV2 PARTS templates must render exactly one root HTML element

Foundry's `_parsePartHTML` (foundry.mjs:32135) rejects any `static PARTS`
template whose rendered output contains more than one root HTML element
— or zero — with `Error: Template part 'X' must render a single HTML
element.` Foundry catches this only at app render time; the JSON parses,
the Handlebars compiles, the app object instantiates fine. The crash
fires on `app.render()`.

Worst part: the failure mode depends on which branch of `{{#if}}/{{else}}`
fires. `sync-calendar.hbs` once had a `degraded` branch that emitted one
`<section>` (clean) and an `else` branch that emitted three siblings
(`<header>`, `<main>`, `<footer>`) — the app worked in degraded mode (no
Calendaria connected, so the degraded branch fired) and crashed the moment
the operator opened it against a real Calendaria.

Same footgun class as F-LANG-1 (silent Foundry-runtime contract that
Node tests can't catch by accident). Fix: wrap the entire template body
in a single root element (e.g. `<div class="sync-calendar-root">`) so
EVERY branch — degraded, year-view, month-view, with or without import
banner — emits exactly one root. **CI guard at
`tools/test-template-roots.mjs`** statically analyzes every PARTS-
registered template across the repo, recurses through every
`{{#if/unless/each/with}}` branch, and asserts max-root-count == 1.

### F-CAL-RAIL-SCROLL: the single-root wrapper must be a height-constrained flex column

Consequence of F-PR3-4. The mandated `<div class="sync-calendar-root">` wrapper
sits between `.sync-calendar-content` (a `height:100%` flex column) and the
header/grid/footer. If the wrapper keeps the default `display:block`, the grid's
`flex:1 1 auto` and the left rail's `overflow-y:auto; min-height:0` have nothing
to resolve against, so the validation **findings panel overflows and visually
overlaps** instead of scrolling (reported as "errors bottom-left I can't read").
Fix (in `styles/chronicle-sync.css`): `.sync-calendar .sync-calendar-root {
display:flex; flex-direction:column; height:100%; min-height:0; }`. Any new
ApplicationV2 single-root wrapper that contains a `flex:1` scroll region needs
the same treatment.

### F-LANG-1: `lang/en.json` cannot mix dotted-flat keys with sibling literal-key strings

Foundry's `Localization._loadTranslationFile` runs the parsed JSON
through `expandObject`, which walks dotted keys as nested paths. If a
sibling key sets `parent.X = "some string"` AND another sibling sets
`parent.X.Y = "..."` (dotted-flat form), `_expand` tries to write a
property on the string and throws `TypeError: Cannot create property
'Y' on string '...'`. **Foundry rejects the entire lang file** — every
`CHRONICLE.*` key renders raw across the whole module.

Worst part: the failure is silent at lint time. The JSON parses fine.
It only blows up at Foundry boot, and Foundry's console message points
at the file, not the offending key pair.

The note-form's `Visibility` field triggered this pattern (`Visibility` +
`Visibility.Visible/Hidden/Secret`, ditto `DisplayStyle`) and rendered raw
for a stretch before anyone noticed. Fix: nest the option set inside a sibling key
(`VisibilityOption: {Visible, Hidden, Secret}`), keeping the field
label `Visibility` as a flat string. **CI guard at
`tools/test-lang-expand.mjs`** simulates `expandObject` and fails loud
on any future collision (both string-vs-object and
duplicate-after-expansion paths).

### F-PR3-3: Sync Calendar editor reaches Chronicle via the public api surface

The Sync Calendar Application does NOT hold a direct reference to the
SyncManager. It looks up the Chronicle REST client through
`game.modules.get('chronicle-sync').api.getAPI()` (the public surface in
`module.mjs:307`). Same pattern external integrators use. Avoids
tight-coupling the editor to SyncManager lifecycle.

### F-SYNC-1: JournalSync must skip JournalEntries owned by another sync domain

`JournalSync`, `NoteSync`, and `CalendarSync` all listen on the same global
`createJournalEntry` hook, and `JournalSync` is registered first (`module.mjs`).
**Calendaria stores every note as a `JournalEntry`** (flagged
`flags.calendaria.isCalendarNote`, in a "Calendar Notes" folder), SimpleCalendar
does too, and `NoteSync` stores Chronicle Notes as JournalEntries. `JournalSync`
used to push **every** new journal lacking an `entityId` flag to `POST /entities`
with `entity_type_id: 0`. The Chronicle server resolves `0` to the campaign's
*first* entity type (`syncapi/api_handler.go` `CreateEntity`) — almost always
"Character" — so the notes showed up in the **Characters** list.

The trigger in the wild: **Calendaria auto-seeds a calendar's `festivals` as
note JournalEntries** the first time the calendar activates
(`festivals/festival-manager.mjs` → `NoteManager.createNote`). A *Calendar of
Therin* with holidays ("Day of Rebirth", "Eve of the Dead", "Haelyn's Day", …)
silently spawned that many "Character" entities. See **`CALENDARIA-INTEGRATION.md`**
for the full Calendaria data model (verified against upstream source).

Rule: before pushing a journal as an entity, `JournalSync` skips it when
`isCalendarNoteJournal(journal)` (exported from `calendar-sync.mjs`; matches the
calendar module's own note flag — `flags.calendaria.isCalendarNote/isCalendarJournal`
or the SimpleCalendar scopes — present on the very first create hook) or
`_isHandledByNoteSync(journal)` is true. Mirrors the existing
`_isHandledByActorSync` guard; same class as the actor duplicate problem that
`cleanupActorJournalDuplicates` cleans up.

Cascade + actor-pull traps when cleaning up the bad entities: the mis-pushed
note's JournalEntry still carries the bogus `entityId` flag, so deleting the
entity in Chronicle broadcasts `entity.deleted`, which
`JournalSync._onEntityDeleted` turns into a **delete of the calendar note in
Foundry** (Calendaria does NOT block this for a GM — `note-manager.mjs`
`onPreDeleteJournalEntry` only protects festival notes from non-GMs). And simply
unlinking the journal without deleting the entity leaves an orphan that
`ActorSync.onSyncMapping` will re-pull as a festival-named Actor. Correct order:
unlink the journal locally **first**, then delete the entity server-side — the
one-off macro in `CALENDARIA-INTEGRATION.md` does exactly this.

Guard logic is unit-tested at `tools/test-calendar-note-journal.mjs`.

## Architectural Decision Log

This section records non-obvious architectural choices so future contributors
don't re-litigate them. Each entry: decision, why, what was rejected, when.

### A-1: Chronicle-served module distribution

**Decision.** This module's release artifacts are served by Chronicle on a
per-campaign signed URL, not by GitHub Releases or Foundry's central Package
Repository.

**Why.** The operator wanted to pin different campaigns to different module
versions, control rollout from Chronicle's admin UI, and not expose unbounded
public install URLs.

**Rejected.** (a) GitHub Releases as the install source — no per-campaign
pinning, no rollout control. (b) Foundry's central Package Repository — same
problems, plus we'd need to keep the in-repo `module.json` URLs synced with
whatever the Package Repository surfaces (drift risk). (c) Embedding Foundry
serving knowledge in Chronicle's core — operator's modularity principle:
*"foundry stuff inside chronicle natively just makes a redundant mess."*

### A-2: Descriptor-driven Chronicle serving

**Decision.** Chronicle reads `chronicle-package.json` from the extracted
release zip to learn how to serve this module — manifest path, fields to
rewrite, URL templates, token requirements. Falls back to hardcoded defaults
if the descriptor is absent.

**Why.** Keeps Chronicle generic — any future Foundry module (or other package
type) can register its own descriptor without modifying Chronicle. The
"Foundry instructs Chronicle" pattern.

**Rejected.** (a) Hardcoding Foundry-specific paths and rewrites in Chronicle.
(b) Inline metadata in `module.json` flags — Foundry's release workflow uses
`jq` to rewrite `module.json`, which would risk clobbering a `flags.chronicle`
block. (c) Per-Chronicle-instance config — punts the problem; operator would
have to keep two configs in sync.

### A-3: Foundry stores Chronicle URL post-install; updates loop back to Chronicle

**Decision.** Foundry's install flow stores the per-campaign manifest URL
locally; subsequent update checks always hit Chronicle (per-campaign signed
URL), never GitHub.

**Why.** The pinned version is owned by Chronicle. Round-tripping update
checks through Chronicle means the campaign owner's version choice is
authoritative.

**Risk + mitigation.** Foundry's central Package Repository also lists this
module with its GitHub manifest URL. On update check, Foundry may prompt to
switch the locally-stored manifest URL to the Package Repository's — see
README's "Updating in Foundry" section.

### A-4: Update Source diagnostic dialog as a manual check

**Decision.** Surface a manual "Check Chronicle for updates" dialog in the
module settings rather than autopatching the install URL or running update
probes on a timer.

**Why.** The dialog is a diagnostic tool, not part of the runtime update
loop. Foundry's native Setup → Modules → Update All is the production update
path. The dialog answers the operator's "is this install wired up correctly?"
question without touching Foundry's stored URLs.

**Rejected.** Auto-correcting the install-time URL — too aggressive; if the
operator deliberately installed from GitHub for some reason, we shouldn't
silently re-point them.
